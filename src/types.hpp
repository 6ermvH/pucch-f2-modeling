#pragma once

#include <complex>
#include <cstdint>
#include <vector>

namespace pucch {

using Bits    = std::vector<uint8_t>;
using LLRs    = std::vector<double>;
using Symbols = std::vector<std::complex<double>>;

}
